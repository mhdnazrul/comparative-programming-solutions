#include <bits/stdc++.h>
#include <cmath>

using namespace std;

int main()
{
  double R, Area, pi = 3.141592653;
  cin >> R;
  Area = pi * pow(R, 2);
  cout << fixed << setprecision(9);
  cout << Area << endl;
  return 0;
}